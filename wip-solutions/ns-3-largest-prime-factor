import math

def is_prime(num):
    prime_list = []
    for i in range(2,int(math.sqrt(num))+1):
        if num % i == 0:
            continue
        prime_list.append(i)

number = 600851475143
primes = is_prime(number)
print(primes)

# for i in primes:
#     y = []
#     if number % i == 0:
#         y.append(i)
# print(y)
