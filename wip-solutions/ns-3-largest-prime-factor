

def is_prime(num):
    for i in range(2,int(num/2)):
        if num % i == 0:
            return False
    return True

def largest_factor(num):
    list = []
    for i in range(2,int(num/2)):
        x = num / i 
        if int(x) == x:
            list.append(x)
    return list

print(largest_factor())

